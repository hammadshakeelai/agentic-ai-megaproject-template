# Copy to .env (never committed). Document every variable the project needs here
# with a placeholder value, so agents and humans know the required configuration
# without ever seeing real secrets.

# EXAMPLE_API_KEY=your-key-here
